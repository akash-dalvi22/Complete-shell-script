#!/bin/bash

# Using variables we can store the output of commands and reuse them later in the script. This can help make our scripts more efficient and easier to read.

current_date=$(date)
echo "Today's date is: $current_date"